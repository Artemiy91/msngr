const toggle=document.querySelector('[data-menu-toggle]'),menu=document.querySelector('[data-menu]'),overlay=document.querySelector('[data-menu-overlay]');
const setMenu=open=>{menu.classList.toggle('is-open',open);overlay.classList.toggle('is-open',open);toggle.classList.toggle('is-open',open);toggle.setAttribute('aria-expanded',open)};
toggle.addEventListener('click',()=>setMenu(!menu.classList.contains('is-open')));overlay.addEventListener('click',()=>setMenu(false));menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'});}}));
document.querySelectorAll('.faq-question').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('.faq-item'),answer=item.querySelector('.faq-answer'),open=item.classList.toggle('is-open');answer.style.maxHeight=open?answer.scrollHeight+'px':0;}));
const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target);}}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const form=document.querySelector('[data-form]'),msg=document.querySelector('[data-form-message]');form.addEventListener('submit',e=>{e.preventDefault();let ok=true;form.querySelectorAll('[required]').forEach(field=>{const valid=field.name==='phone'?/^\+?[0-9\s()\-]{10,}$/.test(field.value.trim()):field.checkValidity();field.classList.toggle('error',!valid);ok=ok&&valid;});msg.textContent=ok?'Спасибо! Мы свяжемся с вами в ближайшее время.':'Проверьте имя и телефон.';msg.style.color=ok?'#078675':'#f04438';if(ok)form.reset();});
